# Server Configuration
PORT=8000
HOST=0.0.0.0
ENV=production

# Logging Configuration
# Levels: debug, info, warning, error
# WICHTIG: In Produktion 'info' oder 'warning' verwenden, um personenbezogene Daten zu schützen
LOG_LEVEL=info

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=nora
DB_SSLMODE=disable

# Keycloak Configuration
KEYCLOAK_URL=https://keycloak.nora-nak.de
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=your-secure-admin-password
KEYCLOAK_MASTER_REALM=master

# Multi-Tenancy Configuration
# DEFAULT_TENANT_SLUG: Der Tenant-Slug, der für diese Backend-Instanz verwendet wird
# Dies ist der Tenant, mit dem sich alle Benutzer authentifizieren
DEFAULT_TENANT_SLUG=nordakademie-hh

# ENABLE_TENANT_SUBDOMAIN: Aktiviert subdomain-basiertes Multi-Tenancy
# false = Ein Tenant pro Backend-Instanz (empfohlen für einfaches Setup)
# true = Mehrere Tenants über Subdomains (z.B. tenant1.domain.de, tenant2.domain.de)
# WICHTIG: Aktuell sollte dies auf 'false' gesetzt werden
ENABLE_TENANT_SUBDOMAIN=false

# SMTP Configuration (optional - Keycloak can handle emails)
# Note: If Keycloak is configured to send emails, these settings are not required
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=nora@nora-nak.de

# Team Notifications
TEAM_EMAIL=team@deine-mail.de

# Frontend URL
FRONTEND_URL=https://nora-nak.de


# ============================================
# REMOVED SETTINGS (No longer needed)
# ============================================
# JWT_SECRET - Keycloak manages JWT keys
# AUTH_MODE - Keycloak handles authentication
# Session settings - JWT is stateless
