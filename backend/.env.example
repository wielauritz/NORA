# Server Configuration
PORT=8000
HOST=0.0.0.0
ENV=production

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=nora
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production-min-32-characters

# SMTP Configuration (for emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=nora@nora-nak.de

# Frontend URL
FRONTEND_URL=https://nora-nak.de

# ICS Import URLs (comma-separated)
# Format: ZENTURIE_NAME=URL
# Example: ICS_I24C=https://example.com/ics/I24c.ics
# ICS_I24C=
# ICS_A24B=
